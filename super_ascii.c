// 'a'=97
#include<stdio.h>
char c[100];
void main()
{  printf("Enter the string in lower ca\n");
   for(i=0;i<100;i++)
   {  c[i]=getchar();
      if(c[i]=='\n')
         break;
   }
// find the total would_be_count by observing the largest value characters
// no chara must be omitted
 
